@model IEnumerable<Kartverket.Web.Models.Entities.Report>

@* ---------------------------------------------------------------------------
    View: My Drafts
    Role: Pilot
    Description: 
        Viser en liste over innlogget brukers påbegynte, men ikke innsendte rapporter.
        Tillater redigering (Edit) og sletting (Delete) av utkast.
        
    Dev Note: 
        Model is currently IEnumerable<Report> (Entity). In a strictly layered 
        architecture (Clean Architecture), this should ideally be mapped to a 
        'DraftListViewModel' to decouple the View from the Data Layer.
    --------------------------------------------------------------------------- 
*@

@{
    ViewData["Title"] = "My Drafts";
}

<section class="mx-auto max-w-6xl pt-10 px-4">
    <div class="flex items-center justify-center gap-4 mb-8">
        @* Url.Content sikrer korrekt sti ved deployment *@
        <img src="@Url.Content("~/images/Kartverketlogo.svg")" alt="Kartverket" class="h-16 w-auto" />
        <h1 class="text-4xl font-medium text-gray-900">My Drafts</h1>
    </div>

    @* Feedback Messages *@
    @if (TempData["SuccessMessage"] != null)
    {
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
            @TempData["SuccessMessage"]
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-6">
            @TempData["ErrorMessage"]
        </div>
    }

    @if (!Model.Any())
    {
        <div class="bg-white shadow-md rounded-2xl p-12 border border-gray-200 text-center">
            <p class="text-gray-600 text-lg">You have no saved drafts.</p>
            <div class="mt-6">
                <a asp-action="DataForm" class="bg-green-600 text-white px-6 py-3 rounded-xl font-semibold hover:bg-green-700">
                    Create New Report
                </a>
            </div>
        </div>
    }
    else
    {
        <div class="bg-white shadow-md rounded-2xl border border-gray-200 overflow-hidden">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase">Title</th>
                        <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase">Height</th>
                        <th class="px-6 py-4 text-left text-xs font-bold text-gray-600 uppercase">Last Saved</th>
                        <th class="px-6 py-4 text-right text-xs font-bold text-gray-600 uppercase">Action</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-100">
                    @foreach (var draft in Model)
                    {
                        <tr class="hover:bg-gray-50">
                            <td class="px-6 py-4">
                                @* Null-coalescing operator (??) håndterer manglende tittel elegant *@
                                <span class="font-medium text-gray-900">@(draft.Title ?? "(Untitled Draft)")</span>
                            </td>
                            <td class="px-6 py-4 text-sm text-gray-700">@draft.HeightInFeet ft</td>
                            <td class="px-6 py-4 text-sm text-gray-500">
                                @* Safe navigation operator (?.) forhindrer kræsj ved manglende timestamp *@
                                @(draft.TimestampEntry?.DateOfLastChange?.ToString("g") ?? "N/A")
                            </td>
                            <td class="px-6 py-4 text-right flex justify-end items-center gap-2">
                                <a asp-action="EditDraft" asp-route-id="@draft.ReportId"
                                   class="inline-flex items-center px-3 py-2 rounded-lg bg-yellow-500 text-white text-sm font-medium hover:bg-yellow-600 transition-colors">
                                    Edit
                                </a>

                                @* SIKKERHET: Sletting gjøres via POST (Form), aldri via GET (Link).
                                    Dette forhindrer at nettleser-plugins eller crawlere sletter data ved uhell,
                                    og sikrer mot CSRF-angrep ved hjelp av AntiForgeryToken.
                                *@
                                <form asp-action="DeleteDraft" asp-route-id="@draft.ReportId" method="post" 
                                      onsubmit="return confirm('Are you sure you want to delete this draft? This cannot be undone.');">
                                    @Html.AntiForgeryToken()
                                    <button type="submit" 
                                            class="inline-flex items-center px-3 py-2 rounded-lg bg-red-600 text-white text-sm font-medium hover:bg-red-700 transition-colors">
                                        Delete
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        
        <div class="mt-8 text-center">
            <a asp-controller="Home" asp-action="MainPage" class="text-gray-600 hover:text-gray-900 font-medium">Back to Home</a>
        </div>
    }
</section>